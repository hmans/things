{
  "name": "hmans-things-monorepo",
  "private": true,
  "version": "1.0.0",
  "main": "index.js",
  "author": "Hendrik Mans <hendrik@mans.de>",
  "license": "MIT",
  "workspaces": {
    "packages": [
      "packages/*",
      "apps/*"
    ]
  },
  "preconstruct": {
    "packages": [
      "packages/*"
    ]
  },
  "prettier": {
    "trailingComma": "none",
    "tabWidth": 2,
    "useTabs": false,
    "semi": false,
    "singleQuote": false,
    "arrowParens": "always",
    "printWidth": 80
  },
  "scripts": {
    "build": "preconstruct build",
    "clean:vite": "rimraf **/node_modules/.vite",
    "dev": "yarn examples",
    "examples": "yarn clean:vite && preconstruct dev && cd apps/examples/ && yarn dev",
    "postinstall": "preconstruct dev && manypkg check",
    "test": "jest --passWithNoTests",
    "ci": "preconstruct validate && manypkg check && yarn build && yarn test",
    "release": "yarn ci && yarn changeset publish"
  },
  "dependencies": {
    "@changesets/cli": "^2.24.3",
    "@manypkg/cli": "^0.19.1",
    "@preconstruct/cli": "^2.1.5",
    "jest": "^28.1.3",
    "jest-environment-jsdom": "^28.1.3",
    "rimraf": "^3.0.2",
    "ts-jest": "^28.0.8"
  },
  "stackblitz": {
    "startCommand": "cd apps/examples && yarn dev"
  }
}
